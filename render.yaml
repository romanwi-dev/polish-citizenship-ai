services:
- type: web
  name: polish-citizenship-intake-v2
  env: python
  region: frankfurt
  plan: free
  runtime:
    pythonVersion: 3.11
  buildCommand: "pip install -r requirements.txt"
  startCommand: "uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  envVars:
  - key: PYTHONUNBUFFERED
    value: "1"
